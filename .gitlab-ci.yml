 before_script:
  - git status

 stages:
   - build
   - test
   - deploy

 job 1:
   stage: build
   script: echo "Build ok test"

 job 2:
   stage: test
   only: [tags]
   script: curl ifconfig.me/all.json
   tags:
    - vps
